name: Refresh FlipHaus data

on:
  schedule:
    - cron: "20 5 * * *"
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Warm up FlipHaus server
        # Wake a sleeping Render instance before the heavy scrape so the first
        # /api/scrape request doesn't hit a cold start (which previously hung
        # for the full 300s --max-time and then failed). /api/scrape-health is
        # public and lightweight (two DB reads, no Puppeteer). Retries cover the
        # dyno spin-up window. Never fails the run — the scrape step has its own
        # retries.
        continue-on-error: true
        run: |
          curl -sS --max-time 60 --retry 6 --retry-all-errors --retry-delay 10 \
            "$FLIPHAUS_REFRESH_URL/api/scrape-health" \
            -o /dev/null -w "warmup HTTP %{http_code} in %{time_total}s\n"
        env:
          FLIPHAUS_REFRESH_URL: ${{ secrets.FLIPHAUS_REFRESH_URL }}

      - name: Refresh active dashboard listings
        run: |
          curl --fail-with-body -sS --max-time 300 --retry 2 --retry-all-errors --retry-delay 20 -H "x-refresh-token: $FLIPHAUS_REFRESH_TOKEN" "$FLIPHAUS_REFRESH_URL/api/scrape?includeDetails=false"
        env:
          FLIPHAUS_REFRESH_URL: ${{ secrets.FLIPHAUS_REFRESH_URL }}
          FLIPHAUS_REFRESH_TOKEN: ${{ secrets.FLIPHAUS_REFRESH_TOKEN }}

      - name: Refresh Farsta sold comparable properties
        run: |
          curl --fail-with-body -sS --max-time 300 --retry 2 --retry-all-errors --retry-delay 20 -H "x-refresh-token: $FLIPHAUS_REFRESH_TOKEN" "$FLIPHAUS_REFRESH_URL/api/scrape-sold?area=Farsta&detailLimit=5&includeDetails=false&includeAnalysis=false"
        env:
          FLIPHAUS_REFRESH_URL: ${{ secrets.FLIPHAUS_REFRESH_URL }}
          FLIPHAUS_REFRESH_TOKEN: ${{ secrets.FLIPHAUS_REFRESH_TOKEN }}

      - name: Refresh expansion-area sold comparable properties
        # The expansion areas in LOCATION_IDS beyond Farsta (2026-06).
        # Looped (not one step each) to keep the workflow compact; --data-urlencode
        # encodes the non-ASCII names (Skarpnäck, Årsta, Hökarängen, Kärrtorp,
        # Högdalen) and the space in "Stora Mossen" (quoted so the for-loop keeps
        # it one token). A blocked area is logged and skipped so one failure doesn't
        # drop the others.
        continue-on-error: true
        run: |
          for area in Kista Bagarmossen Skarpnäck Johanneshov Bromma Alvik "Stora Mossen" Enskede Solna Årsta Hökarängen Kärrtorp Högdalen; do
            echo "→ sold scrape: $area"
            curl --fail-with-body -sS -G --max-time 300 --retry 2 --retry-all-errors --retry-delay 20 \
              -H "x-refresh-token: $FLIPHAUS_REFRESH_TOKEN" \
              --data-urlencode "area=$area" \
              --data-urlencode "detailLimit=5" \
              --data-urlencode "includeDetails=false" \
              --data-urlencode "includeAnalysis=false" \
              "$FLIPHAUS_REFRESH_URL/api/scrape-sold" \
              || echo "  ✗ $area sold scrape failed (continuing)"
          done
        env:
          FLIPHAUS_REFRESH_URL: ${{ secrets.FLIPHAUS_REFRESH_URL }}
          FLIPHAUS_REFRESH_TOKEN: ${{ secrets.FLIPHAUS_REFRESH_TOKEN }}

      - name: Analyse scraped listing images
        continue-on-error: true
        run: |
          curl --fail-with-body -sS --max-time 300 --retry 1 --retry-all-errors --retry-delay 20 -H "x-refresh-token: $FLIPHAUS_REFRESH_TOKEN" "$FLIPHAUS_REFRESH_URL/api/analyze-images?dataset=all&limit=10"
        env:
          FLIPHAUS_REFRESH_URL: ${{ secrets.FLIPHAUS_REFRESH_URL }}
          FLIPHAUS_REFRESH_TOKEN: ${{ secrets.FLIPHAUS_REFRESH_TOKEN }}

      - name: Confirm sold FlipHaus dashboard listings
        run: |
          curl --fail-with-body -sS -H "x-refresh-token: $FLIPHAUS_REFRESH_TOKEN" "$FLIPHAUS_REFRESH_URL/api/reconcile-sold"
        env:
          FLIPHAUS_REFRESH_URL: ${{ secrets.FLIPHAUS_REFRESH_URL }}
          FLIPHAUS_REFRESH_TOKEN: ${{ secrets.FLIPHAUS_REFRESH_TOKEN }}
